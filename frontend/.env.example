# Base URL of the GGUF Editor FastAPI backend, including the `/api` prefix.
# Read at build time by the browser bundle, so it must be reachable from the
# user's browser (not from inside the container).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
